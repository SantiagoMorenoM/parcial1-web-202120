Se creó una función en el endpoint tanto para conseguir todos los jugadores como para conseguir las parejas.
El servidor corre en el localhost:3000
Para conseguir los jugadores se utiliza axios, se mandan al controller que tiene los métodos, y para encontrar las parejas
se utiliza la altura como parametro